# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Backups
*.bak
*.bak-*
.backups/

# Provenance local (merge history, no se sube al repo)
reusado/

# Workspace de merge: copias de ENTRADA (fuentes de solo lectura) y
# la pre-copia de salida. Se regeneran, no se commitean.
# Sí se commitean: merge/COLA.md y merge/informes/ (el razonamiento).
merge/dist/
merge/aporte/
merge/salida/

# Node (futuro)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
